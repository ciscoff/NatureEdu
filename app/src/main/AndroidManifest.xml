<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="android.hardware.camera.any" />
    <uses-feature android:name="android.hardware.camera.flash" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Main"
        tools:targetApi="31">
        <activity
            android:name=".video.camera.CameraActivity"
            android:exported="false" />
        <activity
            android:name=".video.distortion.VideoDistortionActivity"
            android:exported="false" />
        <activity
            android:name=".sphere.main.ColSphereActivity"
            android:exported="false" />
        <activity
            android:name=".sphere.main.TexSphereActivity"
            android:exported="false" />
        <activity
            android:name=".sphere.planet.PlanetActivity"
            android:exported="false" />
        <activity
            android:name=".sphere.ico.IcoActivity"
            android:exported="false" />
        <activity
            android:name=".sphere.globe.GlobeActivity"
            android:exported="false" />
        <activity
            android:name=".mountains.MountainsActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>